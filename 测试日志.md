# 测试日志

## 2026-05-04 — 项目初始化阶段

### 测试状态
- 阶段：骨架代码，尚未编写测试
- 覆盖率：0%（待 Phase 1 开始编写单元测试）

### 待测试项
- [ ] claude-bridge: 双 Map 架构、findActiveSession、checkSessionAlive、terminateSession
- [ ] approval-store: 原子写入、create/get/resolve、findByShortId、重启后 TIMEOUT 标记
- [ ] approval-server: HTTP 轮询架构、/request /status /respond 路由、重启通知
- [ ] approval-rules: YAML 加载、正则安全检测、规则匹配逻辑
- [ ] git-snapshot: stash 创建/回滚、message 定位、会话状态验证清理
- [ ] hook-inbox: 配置写入、OS 脚本选择、matcher 参数、模式切换
- [ ] persistent-session-manager: 复合键隔离、activate/deactivate、原子持久化
- [ ] context-manager: CLAUDE.md 注入/清理、orphan 检测、临时上下文构建
- [ ] utils: atomicWriteSync、safeLoadJson
- [ ] integration: 审批流程端到端、会话生命周期、回滚流程

---

## 2026-05-04 — Phase 1 测试完成

### 测试结果
- 总测试数：144
- 通过：144
- 失败：0
- 测试文件：10（9 个 core 模块 + 1 个 plugin）

### 覆盖率（src/core/ 目录）
| 模块 | 语句 | 分支 | 函数 | 行 |
|------|------|------|------|------|
| 全体 | 98.88% | 90.96% | 98.43% | 99.41% |
| approval-rules.js | 100% | 100% | 100% | 100% |
| approval-server.js | 100% | 88.46% | 100% | 100% |
| approval-store.js | 100% | 87.5% | 100% | 100% |
| claude-bridge.js | 98.64% | 89.18% | 91.66% | 100% |
| context-manager.js | 98.03% | 87.5% | 100% | 97.91% |
| git-snapshot.js | 95.45% | 88.88% | 100% | 97.5% |
| hook-inbox.js | 100% | 80% | 100% | 100% |
| persistent-session-manager.js | 100% | 100% | 100% | 100% |
| utils.js | 100% | 100% | 100% | 100% |

### 测试覆盖要点
- claude-bridge: findActiveSession 多场景、checkSessionAlive 存活/退出/缺失、spawnSession 创建/复用/参数传递、terminateSession 进程清理/锁释放、心跳启停/超时检测、exit 事件回调、updateActivity
- approval-store: create 持久化、get/findByShortId、resolve APPROVED/DENIED、markAllPendingAsTimeout、_load 重启 TIMEOUT 标记、_loadTimedOutCount、cleanup 过期清理
- approval-server: HTTP 三路由(request/status/respond)、无效 JSON 400、未知路由 404、onApprovalNeeded 回调、stop 标记 TIMEOUT、notifyTimedOutRequests 重启通知
- approval-rules: 加载校验(长度/嵌套量词/反向引用/编译失败)、match 工具/命令/路径/通配符、敏感标记
- git-snapshot: create 正常/无会话/非 git 目录、revert 正常/无 stashRef/已删除/失败、cleanupOldStashes 活跃保留/过期删除/空列表/非 CC-snapshot 跳过
- hook-inbox: writeHookConfig 默认 Bash/自定义 matcher/环境变量/路径存储、updateMatcher
- persistent-session-manager: getKey 复合键、activate/deactivate/getActive 持久化
- context-manager: injectRules 标记段/防重复/创建文件、removeInjectedRules、cleanup、cleanOrphanedRules 无活跃/有活跃/无标记/无文件、buildContextPrompt
- utils: atomicWriteSync 创建目录/覆盖/无残留 tmp、safeLoadJson 有效/无效/不存在、文件锁获取/释放
- plugin: formatToolInput 各种工具、init 注册 9 个命令、各命令 handler 基本场景

---

## 2026-05-04 — Phase 2 测试完成

### 测试结果
- 总测试数：180
- 通过：180
- 失败：0
- 测试文件：11（9 个 core 模块 + 1 个 plugin + 1 个 integration）

### 覆盖率（src/core/ 目录）
| 模块 | 语句 | 分支 | 函数 | 行 |
|------|------|------|------|------|
| 全体 | 98.94% | 91.81% | 98.5% | 99.43% |
| approval-rules.js | 100% | 100% | 100% | 100% |
| approval-server.js | 100% | 92.1% | 100% | 100% |
| approval-store.js | 100% | 90% | 100% | 100% |
| claude-bridge.js | 98.64% | 89.18% | 91.66% | 100% |
| context-manager.js | 98.03% | 87.5% | 100% | 97.91% |
| git-snapshot.js | 95.45% | 88.88% | 100% | 97.5% |
| hook-inbox.js | 100% | 80% | 100% | 100% |
| persistent-session-manager.js | 100% | 100% | 100% | 100% |
| utils.js | 100% | 100% | 100% | 100% |

### 新增测试
- approval-store: findBySessionId（按会话查询、空结果）、listPending（仅 PENDING、无 PENDING、空 store）
- approval-server: 规则引擎集成（efficient 模式 Write/Edit/Bash 安全命令自动放行、Bash 破坏性命令 PENDING、不触发 onApprovalNeeded）；strict 模式覆盖（Write/Edit PENDING、Bash 安全命令仍自动放行）；setMode 方法；无规则配置时全部 PENDING
- plugin: cc_mode 模式切换同步 approvalServer.setMode、onApprovalNeeded 回调格式化通知（formatToolInput 预览）
- integration/approval-flow: efficient 模式端到端流程（自动审批 + 手动审批/拒绝）、strict 模式覆盖、模式切换影响行为、formatToolInput 预览、findBySessionId/listPending 集成、服务停止 TIMEOUT 标记、异常重启超时通知

---

## 2026-05-04 — Phase 3 测试完成

### 测试结果
- 总测试数：197
- 通过：197
- 失败：0
- 测试文件：12（9 个 core 模块 + 1 个 plugin + 2 个 integration）

### 覆盖率（src/core/ 目录）
| 模块 | 语句 | 分支 | 函数 | 行 |
|------|------|------|------|------|
| 全体 | 98.76% | 91.62% | 98.55% | 99.21% |
| approval-rules.js | 100% | 100% | 100% | 100% |
| approval-server.js | 100% | 92.1% | 100% | 100% |
| approval-store.js | 100% | 90% | 100% | 100% |
| claude-bridge.js | 98.64% | 89.18% | 91.66% | 100% |
| context-manager.js | 98.07% | 83.33% | 100% | 97.95% |
| git-snapshot.js | 95.71% | 91.66% | 100% | 96.77% |
| hook-inbox.js | 100% | 80% | 100% | 100% |
| persistent-session-manager.js | 100% | 100% | 100% | 100% |
| utils.js | 100% | 100% | 100% | 100% |

### 新增测试
- git-snapshot: revert 后 stashRef 自动清除（revert 成功后 stashRef 变为 null，二次 revert 失败）；dropStash（删除快照+清除 stashRef、无 stashRef 时失败、stash 已手动删除时安全处理）
- context-manager: buildContextPrompt 跨平台目录列表命令（验证使用 ls -la 或 dir /b）
- plugin: cc_revert 无快照提示（stashRef 为 null 时返回"无可用的快照"、--confirm 时同样检查）
- integration/revert-context-flow: 快照+回滚生命周期（创建快照→CC 修改→回滚→stashRef 清除→二次 revert 失败）；dropStash 清理（删除快照+验证 stash 列表）；CLAUDE.md 注入+清理生命周期（inject→cleanup→验证标记移除）；孤立规则清理（无活跃会话自动清理、有活跃会话保留）；完整 cc_start→cc_revert→cc_stop 端到端流程；cleanupOldStashes 活跃/非活跃会话区分；buildContextPrompt git 仓库/非 git 目录

---

## 2026-05-04 — Phase 4 测试完成

### 测试结果
- 总测试数：261
- 通过：261
- 失败：0
- 测试文件：15（9 个 core 模块 + 1 个 plugin + 3 个 integration + 2 个新增模块）

### 覆盖率（src/core/ 目录）
| 模块 | 语句 | 分支 | 函数 | 行 |
|------|------|------|------|------|
| 全体 | 98.94% | 91.96% | 98.78% | 99.32% |
| approval-rules.js | 100% | 100% | 100% | 100% |
| approval-server.js | 100% | 92.1% | 100% | 100% |
| approval-store.js | 100% | 95% | 100% | 100% |
| claude-bridge.js | 98.64% | 89.18% | 91.66% | 100% |
| command-parser.js | 100% | 100% | 100% | 100% |
| context-manager.js | 98.07% | 83.33% | 100% | 97.95% |
| feishu-messenger.js | 100% | 88.57% | 100% | 100% |
| git-snapshot.js | 95.71% | 91.66% | 100% | 96.77% |
| hook-inbox.js | 100% | 80% | 100% | 100% |
| persistent-session-manager.js | 100% | 100% | 100% | 100% |
| utils.js | 100% | 100% | 100% | 100% |

### 新增测试
- feishu-messenger: sendToUser 短消息/长消息分片/空消息/null 消息；splitMessage 短文本/换行分割/代码块边界保护/精确长度/硬分割；formatCodeBlock 带语言/不带语言；formatApprovalNotification 完整通知；formatToolProgress 成功/错误/进行中/截断长详情；formatErrorMessage 字符串/Error/null/截断；formatSessionStatus 存活/已退出；countCodeBlockMarkers 零/一个/成对
- command-parser: 10 个命令解析（cc, cc_start, cc_stop, cc_status, cc_answer, cc_approve, cc_deny, cc_revert, cc_context, cc_mode）；参数提取（带空格参数、短ID、模式参数）；非命令/null/undefined/未知命令；额外空白/前后空白处理；getCommands 返回列表和副本；getHelpText 包含所有命令
- integration/e2e-session-flow: 完整审批生命周期（命令解析→创建请求→格式化通知→批准/拒绝→状态确认）；规则自动审批（Write efficient 模式、git status 安全命令）；消息格式化和分片（长输出分片重组、错误格式化、工具进度）；命令解析边界（全部命令、/cc prompt、未知命令、非命令文本）；模式切换影响（strict 模式 Write PENDING、efficient 模式 Write APPROVED）；服务异常重启超时通知

---

## 2026-05-04 — 安全审查 + 代码修复测试

### 测试结果
- 总测试数：266
- 通过：266
- 失败：0
- 测试文件：15

### 修复后新增/更新测试
- approval-server: sharedSecret 认证（无 auth header→403、正确 Bearer→200）
- approval-store: findByShortId 模糊匹配（多匹配返回 ambiguous、单匹配返回 item、无匹配返回 null）
- context-manager: buildContextPrompt 使用 fs.readdirSync（不再调用 execSync 目录列表命令）
- claude-bridge: 常量重命名 DEFAULT_HEARTBEAT_INTERVAL/DEFAULT_SESSION_TIMEOUT
- git-snapshot: sessionId 格式适配 SESSION_ID_REGEX（cc-<digits>-<alphanumeric>）

### 遇到的问题
- claude-bridge.test.js import 常量双重前缀：`DEFAULT_DEFAULT_HEARTBEAT_INTERVAL`，需修正为 `DEFAULT_HEARTBEAT_INTERVAL`
- context-manager.test.js buildContextPrompt 断言不匹配：execSync 全部 mock 抛异常时，fs.readdirSync 不受 mock 影响仍能列出目录，`expect(prompt).toBe('')` → `expect(prompt).toContain('工作目录内容:')`；同时"验证使用 dir /b/ls -la 命令"的测试需改为验证不调用 execSync 做目录列表
- git-snapshot + revert-context-flow cleanupOldStashes 失败：sessionId 格式 `inactive-session` 不匹配 SESSION_ID_REGEX（`cc-\\d+-[a-z0-9]+`），regex 解析失败导致 stash 不被识别；改用 `cc-1709000000-xyz789` 格式

---

## 2026-05-04 — OpenClaw 飞书消息路由修复测试

### 测试结果
- 总测试数：267
- 通过：267
- 失败：0
- 测试文件：15

### 修复后新增/更新测试
- feishu-messenger: sendToUser 改为 async，mock api 使用 `api.runtime.channel.outbound.loadAdapter` 替代 `api.sendMessage`；新增 accountId 传递测试
- e2e-session-flow: mockApi 改用 outbound adapter mock（`loadAdapter → { sendText }`）；sentMessages 断言从 `.target` 改为 `.to`；`await messenger.sendToUser(...)` 替代同步调用；restartApi 同样添加 runtime.channel.outbound mock
- plugin: mockApi 添加 `api.runtime.channel.outbound` mock；onApprovalNeeded 测试改为验证 `loadAdapter` 被调用而非验证 `sendMessage`