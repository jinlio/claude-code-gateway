# 开发日志

## 2026-05-04 — OpenClaw 插件兼容性修复（ESM 入口 + inbound_claim 钩子）

### 完成内容
- 创建 ESM 入口文件 `src/plugin/entry.mjs`：使用 `export default plugin` + `register(api)` 模式，兼容 OpenClaw >= 2026.3.24-beta.2
- 重写 CJS 入口文件 `src/plugin/index.js`：从 `init(api, config)` 改为 `register(api)` 模式
- **命令处理方式变更**：从 `api.registerCommand()` 改为 `api.on('inbound_claim', handler, { priority: 100 })` 拦截 `/cc` 开头的消息
- 更新 `package.json`：`openclaw` 元数据增加 `runtimeExtensions`、`compat.minGatewayVersion`、`build.openclawVersion`
- 更新 `openclaw.plugin.json`：增加 `activation: { onStartup: true }`、`contracts.commands`（10 个命令声明）、`sharedSecret` 配置项
- 更新插件测试 `test/core/plugin.test.js`：`init` → `register`，测试 `inbound_claim` 钩子拦截行为，`handleCommand()` 直接测试

### 关键决策
- ESM 入口采用 plain object + `export default` 而非 `definePluginEntry()`：避免依赖 `@openclaw/plugin-sdk` npm 包，plain object 与 `definePluginEntry` 返回结构等效
- CJS core 模块保持不变：ESM `.mjs` 入口通过 Node.js 原生 CJS→ESM 互操作 import CJS 模块，核心模块无需改动
- `register(api)` 为同步函数：异步服务启动（ApprovalServer.start）放在 `api.on('gateway_start')` 生命周期钩子中
- **命令处理使用 `inbound_claim` 钩子而非 `registerCommand`**：OpenClaw 2026.5.2 的 `registerCommand` API 与插件 SDK 不兼容（调用报错 "Command handler must be a function"），`inbound_claim` 是官方推荐的消息拦截机制
- `inbound_claim` handler 只拦截 `/cc` 开头的消息，其他消息返回 `undefined` 让 agent 正常处理
- 命令消息返回 `{ handled: true, reply: { text: '...' } }` 阻止 agent turn 并直接回复用户
- 路由字段使用 `senderId`（匹配 `PluginHookInboundClaimEvent.senderId`）而非 `userId`

### 遇到的问题
- **`registerCommand` 不兼容**：OpenClaw 2026.5.2 的 `registerCommand` API 类型定义中只有命令元数据字段（name, nativeNames），没有 handler/execute 属性；调用时报 "Command handler must be a function" 错误（10 个命令均失败）
- **`execute` 属性名不正确**：初次修复时将 `handler` 改为 `execute`，但 OpenClaw 实际不接受该属性名作为命令处理器
- **`inbound_claim` 事件字段名不兼容**：OpenClaw 当前版本使用 `event.CommandBody`（PascalCase）而非 `event.content` 传递命令文本，导致所有 `/cc` 命令穿透到主助手，插件从未拦截。修复为 `(event.CommandBody || event.commandBody || event.content || '').trim()` 兼容多种字段命名
- OpenClaw `PluginHookInboundClaimEvent` 字段：`CommandBody`, `senderId`, `channel`, `accountId`, `isGroup`；context 字段：`channelId`, `accountId`, `senderId`
- `workspace` 字段不在事件 payload 中，使用 `config.workspace || process.cwd()` 作为默认值
- `accountId` 从 `ctx.accountId` 获取（`PluginHookInboundClaimContext` 中包含该字段）
- **安装依赖缺失**：OpenClaw 插件安装流程不自动执行 `npm install`，导致 `proper-lockfile`、`minimatch`、`js-yaml` 找不到。添加 `openclaw.install.defaultChoice: "npm"` 配置引导依赖安装

---

## 2026-05-04 — 项目初始化

### 完成内容
- 创建项目结构 (claude-code-gateway)
- 基于 cc-bridge-v3-final-plan.md 完成核心模块骨架代码
- 完成的模块：claude-bridge.js, approval-server.js, approval-store.js, approval-rules.js, git-snapshot.js, hook-inbox.js, persistent-session-manager.js, context-manager.js, utils.js
- 完成的脚本：cc-bridge-approval-hook.sh (bash), cc-bridge-approval-hook.mjs (node)
- 完成的配置：cc-approval-rules.yml, openclaw.plugin.json, jest.config.js

### 关键决策
- 项目名取为 claude-code-gateway，区别于原始 openclaw-cc-bridge
- 默认 efficient 模式（Edit/Write 免审批），通过 /cc_mode 可切换 strict
- 审批采用两阶段轮询架构而非 HTTP 长连接
- CLAUDE.md 采用标记段注入机制（启停时自动注入/清理）
- Git 快照用 stash + message 定位而非 commit + reset

---

## 2026-05-04 — Phase 1 完成：进程管理 + 文件锁 + 单元测试

### 完成内容
- ClaudeBridge 增强：spawnSession（子进程创建 + 双 Map 注册）、exit 事件监听主动清理、心跳定时器、会话超时自动停止、workspace 文件锁互斥、updateActivity 活跃度追踪
- utils.js 增强：acquireWorkspaceLock / releaseWorkspaceLock（proper-lockfile）、atomicWriteSync 自动创建父目录
- plugin/index.js 完善：添加 path 导入、实现所有 9 个命令处理器（cc_start/stop/status/answer/approve/deny/revert/context/mode）、formatToolInput 工具输入格式化
- 审批规则 approval-rules.js 修复：minimatch v9 导入方式改为 `{ minimatch }` 解构
- Git 快照 git-snapshot.js 修复：stash message 正则从 `(\w+)` 改为 `(.+)` 以支持含连字符的 sessionId
- jest.config.js 调整：collectCoverageFrom 仅覆盖 src/core/ 目录（plugin 依赖飞书 API 无法单元测试）
- 全部 10 个测试文件编写完成（9 个 core 模块 + 1 个 plugin）

### 关键决策
- spawnSession 使用 workspace 级文件锁防止同一目录并发会话
- 心跳定时器按需启停：有活跃会话才启动，无活跃会话自动停止
- minimatch v9 为 ESM+CJS 双格式包，require 返回对象而非函数，需解构导入
- plugin/index.js 单元测试通过 mock 所有 core 模块实现，避免飞书 API 依赖
- stash message 正则使用贪婪 `(.+)` 匹配 sessionId（含连字符），`-(\d+)` 拼接末尾时间戳

---

## 2026-05-04 — Phase 2 完成：审批系统集成增强

### 完成内容
- ApprovalStore 增强：新增 findBySessionId（按会话ID查询审批列表）、listPending（列出所有 PENDING 审批）；_flush 改用 utils.atomicWriteSync 统一原子写入
- ApprovalServer 增强：集成 ApprovalRules 规则引擎，请求到达时先进行规则匹配——auto_approve 直接返回 { approvalId: null, status: 'APPROVED', autoApproved: true } 而不创建 PENDING 记录；新增 setMode 方法支持 efficient/strict 模式切换；strict 模式覆盖 Write/Edit 的 auto_approve 规则，强制 require_approval
- Hook 脚本增强：Bash 和 Node 版本均增加 autoApproved 响应处理——当服务端返回 { status: 'APPROVED', autoApproved: true } 时立即 exit 0，不进入轮询
- Plugin 集成增强：onApprovalNeeded 回调使用 formatToolInput 格式化工具输入预览，发送飞书审批通知卡片（含工具名、内容预览、审批/拒绝命令）；/cc_mode 命令调用 approvalServer.setMode() 同步模式到服务端
- 集成测试：新增 test/integration/approval-flow.test.js，覆盖 efficient 模式自动审批、strict 模式覆盖、模式切换影响行为、审批/拒绝完整流程、会话查询、服务停止标记 TIMEOUT、重启超时通知

### 关键决策
- 规则匹配在 HTTP 请求层实现（ApprovalServer 内部），而非 Hook 脚本层——减少 Hook 调用次数（auto_approve 不创建 PENDING）
- strict 模式仅覆盖 Write/Edit 的 auto_approve，不改变 Bash 的安全命令规则（git status 仍自动放行）
- autoApproved 响应格式 { approvalId: null, status: 'APPROVED', autoApproved: true } 明确区分规则自动放行和手动批准
- ApprovalServer 构造函数扩展为 (dataDir, port, notifyCallback, approvalRules, currentMode)，保持向后兼容（新参数均可选，默认值保持原有行为）

### 遇到的问题
- 集成测试 restart 通知测试场景需区分"正常停机"（stop() 标记 TIMEOUT 并 flush）和"异常停机"（遗留 PENDING），前者重启后不会有通知，后者才会触发；测试改为模拟异常停机（直接关闭 server 而不调用 stop()）

### 遇到的问题
- minimatch v9 导入方式变化导致 TypeError，需改为 `{ minimatch }` 解构
- git stash push 在干净工作目录下不会创建 stash 条目，测试需先制造工作区变更
- approval-server afterEach 清理顺序问题：stop() 写文件时 tmpDir 已被删除，改为安全清理
- stash message 正则 `(\w+)` 不匹配含连字符的 sessionId（如 test-session-1），改为 `(.+)`

---

## 2026-05-04 — Phase 3 完成：回滚 + 上下文集成增强

### 完成内容
- GitSnapshot 增强：revert 成功后自动清除 stashRef（防止重复回滚）；新增 dropStash 方法（cc_stop 时删除快照）；revert 前自动清理工作区修改（git checkout -- . + git clean -fd），解决 stash apply 与工作区冲突问题；cleanupOldStashes 改为逆向遍历 + dropped 计数器避免 drop 后索引偏移
- ContextManager 增强：injectRules/cleanup/cleanOrphanedRules 统一改用 atomicWriteSync 原子写入；buildContextPrompt 跨平台兼容（Windows 用 `dir /b` 替代 `ls -la`）
- Plugin 集成增强：cc_start 时自动调用 cleanOrphanedRules 清理残留规则、创建 GitSnapshot 快照、注入 CLAUDE.md 上下文规则；cc_stop 时自动 dropStash 清理快照、cleanup 清理注入规则、cleanupOldStashes 定期清理过期快照；cc_revert 增强——无快照时明确提示"无可用的快照"、成功回滚后 stashRef 自动清除
- 集成测试：新增 test/integration/revert-context-flow.test.js（10 个测试用例），覆盖快照创建+回滚+stashRef 清理生命周期、dropStash 清理、CLAUDE.md 注入+清理生命周期、孤立规则自动清理/保留、完整 cc_start→cc_revert→cc_stop 端到端流程、cleanupOldStashes 活跃/非活跃会话区分、buildContextPrompt git 仓库/非 git 目录

### 关键决策
- revert 前自动 git checkout + git clean 确保工作区干净：revert 语义是"丢弃快照后的所有修改"，先清理再 apply 避免冲突
- revert 后清除 stashRef 而不 drop stash：保留 stash 供回退查看，但防止重复 revert（二次 revert 返回"无可用快照"）
- cc_stop 时主动 drop stash 而非等待定期清理：会话结束时用户明确不再需要快照，立即释放空间
- cleanupOldStashes 逆向遍历：从 stash 列表末尾开始 drop，避免 drop 操作导致后续索引偏移

### 遇到的问题
- git stash apply 在工作区有未提交修改时失败（"Your local changes would be overwritten by merge"），需在 apply 前先 checkout + clean 清理工作区
- 集成测试中 CLAUDE.md 被 stash 走导致文件消失：解决方案是先 injectRules 再 commit CLAUDE.md，确保它成为已跟踪文件
- 测试间状态泄漏：beforeEach 需要额外清理 CLAUDE.md 和测试文件

---

## 2026-05-04 — Phase 4 完成：飞书集成 + 联调

### 完成内容
- FeishuMessenger 新模块：双向消息转发、代码块格式化、长消息自动分片（含代码块边界保护）、审批通知格式化、工具进度格式化（✓/✗/→）、错误消息格式化、会话状态格式化
- CommandParser 新模块：10 个命令解析（cc, cc_start, cc_stop, cc_status, cc_answer, cc_approve, cc_deny, cc_revert, cc_context, cc_mode）、参数提取、未知命令处理、帮助文本生成
- Plugin 集成增强：集成 FeishuMessenger 和 CommandParser；Claude 进程 stdout/stderr 输出捕获和行级转发；/cc <prompt> 一次性任务命令（复用活跃会话或创建临时会话）；setupProcessForwarding 统一进程输出转发；notifyCallback 支持 restart_timeout 广播所有活跃会话；config 支持 defaultMode/sessionTimeout/heartbeatInterval/maxMessageLength 配置项
- Plugin 测试更新：命令数量从 9 更新为 10（新增 /cc）；cc_start 已有会话时改用 formatSessionStatus 展示状态
- E2E 集成测试：新增 test/integration/e2e-session-flow.test.js（14 个测试用例），覆盖完整审批生命周期（解析→请求→通知→批准/拒绝）、规则自动审批、消息格式化和分片、命令解析边界、模式切换影响行为、服务异常重启超时通知

### 关键决策
- FeishuMessenger 独立为 core 模块而非内联在 plugin 中：格式化和分片逻辑有独立测试价值，未来可被其他消息渠道复用
- 消息分片在换行符处分割（搜索范围：maxLength-200 到 maxLength），找不到换行符时硬分割；代码块边界保护：分割点落在未闭合代码块内时回退到代码块起始位置
- setupProcessForwarding 按行刷新 stdout 缓冲区：完整行立即转发，不完整行留在 buffer 等待更多数据；进程退出时 flush 剩余 buffer
- /cc <prompt> 命令：有活跃会话时直接写入 stdin（复用），无活跃会话时 spawn 新进程并注册输出转发
- 服务异常重启通知测试：需模拟异常停机（关闭 server 而不调用 stop()），因为 stop() 会先标记 PENDING→TIMEOUT 再 flush，导致新服务器 _load 时不再有 PENDING 条目可计数

### 遇到的问题
- Plugin 测试因 /cc 命令新增而失败：注册命令数从 9 变为 10，测试需同步更新
- cc_start 已有活跃会话时的响应从"已有活跃会话"改为 formatSessionStatus 状态展示，测试需适配
- E2E 超时通知测试：FeishuMessenger 持有 api 引用，重置 sentMessages 变量不会影响 messenger 内部引用，需为重启服务器创建独立的 messenger 和 messages 数组

---

## 2026-05-04 — 安全审查 + 代码修复（Critical & High 级别）

### 完成内容
- **Critical 修复 (C1-C6)**
  - C1: git-snapshot.js shell 注入 — stashRef 插值前 sanitize（`replace(/[^a-zA-Z0-9_.-]/g, '_')`），regex 从贪婪 `(.+)` 改为精确 `(cc-\\d+-[a-z0-9]+)`，stash 索引增加整数范围校验
  - C2: context-manager.js command 注入 — 替换 `execSync('dir /b' / 'ls -la')` 为 `fs.readdirSync`，消除 shell 注入风险
  - C3: hook.sh JSON 注入 — 使用 `jq -n --arg` 安全构建 JSON，fallback 用 sed 转义双引号
  - C4: hook.mjs fail-closed — 服务不可达时 `process.exit(2)` 而非 `process.exit(0)`，无 approvalId 也 exit 2
  - C5: approval-server.js respond 端点认证 — 新增 sharedSecret 参数，`/api/approval/respond` 需 `Authorization: Bearer <secret>` 否则返回 403；移除 async handler 改为同步 + try-catch 兜底 500
  - C6: hook.sh fail-closed — 同 C4，bash 版本服务不可达时 exit 2

- **High 修复 (H7-H29)**
  - H7: ApprovalStore 构造函数自动调用 `_load()` 加载持久化数据
  - H8: claude-bridge.js 心跳 Map 并发修改 — 遍历 `[...this.sessionMeta.entries()]` 快照而非直接迭代
  - H10: utils.js atomicWriteSync Windows 兼容 — `renameSync` 失败时 unlink 目标文件再重试
  - H11: cc_approve/cc_deny 权限检查 — 仅会话创建者可以审批/拒绝
  - H12: ApprovalStore.findByShortId 模糊匹配检测 — 多个匹配返回 `{ ambiguous: true, matches }`，cc_approve/cc_deny 提示使用更长的 ID
  - H13: checkSessionAlive 锁泄漏 — 进程退出或缺失时释放 workspace lock
  - H14: approval-server.js HTTP handler — 移除 async，改为同步 try-catch 兜底
  - H15: approval-rules.js 预编译 regex — `loadRules()` 将 regex 存入 `rule._compiledRegex`，`match()` 使用编译结果而非每次 new RegExp
  - H20: plugin/index.js contextManager — 从单实例改为 `Map(workspace → ContextManager)`，支持多工作目录
  - H24: claude-bridge.js 配置可传参 — `constructor(options)` 接收 heartbeatInterval/sessionTimeout，移除无效的 `bridge._sessionTimeout` 外部赋值
  - H29: hook-inbox.js Windows 路径空格 — hook 命令路径加引号 `"..."`
  - FeishuMessenger splitMessage — `splitAt = Math.max(splitAt, 1)` 防止 splitAt 为 0 时无限循环

### 关键决策
- fail-closed 安全模型：所有不可达/不可判定场景均拒绝操作（exit 2/3），而非默认放行（exit 0）
- sharedSecret 认证仅保护 `/api/approval/respond`（写操作），`/api/approval/request` 和 `/api/approval/status`（读操作）不需认证——hook 脚本只调用读端点
- findByShortId 模糊检测返回特殊对象而非 null——调用方需显式处理 ambiguous 情况
- ClaudeBridge 配置从外部赋值改为构造参数——消除类外访问私有属性的问题
- 常量重命名：HEARTBEAT_INTERVAL→DEFAULT_HEARTBEAT_INTERVAL, SESSION_TIMEOUT→DEFAULT_SESSION_TIMEOUT，匹配"可配置默认值"语义

### 遇到的问题
- claude-bridge.test.js 常量重命名后 import 出错：`HEARTBEAT_INTERVAL`→`DEFAULT_HEARTBEAT_INTERVAL` 替换时误生成双重前缀 `DEFAULT_DEFAULT_HEARTBEAT_INTERVAL`，需手动修正 import 语句
- context-manager.test.js 断言不匹配：buildContextPrompt 改用 `fs.readdirSync` 后，当 execSync mock 全部抛异常时，原测试期望 `prompt === ''`，但 fs.readdirSync 不受 mock 影响仍能正常列出目录内容，需改为 `expect(prompt).toContain('工作目录内容:')`；同时"验证使用 dir /b 或 ls -la 命令"的测试需改为验证不调用 execSync 做目录列表
- git-snapshot.test.js + revert-context-flow.test.js cleanupOldStashes 失败：sessionId 格式 `inactive-session` 不匹配 SESSION_ID_REGEX（`cc-\\d+-[a-z0-9]+`），regex 解析失败导致 stash 不被识别和清理；需改用格式为 `cc-1709000000-xyz789` 的 sessionId
- ApprovalStore 构造函数加 `_load()` 后，_loadTimedOutCount 测试中的 `new ApprovalStore(tmpDir)._load()` 调用变为冗余（构造函数已自动调用），但测试仍通过因为重复 _load 只是重新加载

---

## 2026-05-04 — OpenClaw 飞书消息路由修复

### 完成内容
- FeishuMessenger 重写：`sendToUser` 从同步改为 async，使用 `api.runtime.channel.outbound.loadAdapter(channelId).sendText` 替代不存在的 `api.sendMessage`
- Plugin 路由上下文存储：新增 `sessionRoutes` Map（sessionId → { channel, from, to, accountId }），从命令处理器的 `ctx` 参数中提取通道路由信息，用于异步消息投递
- setupProcessForwarding 改用 sessionRoutes 路由：stdout/stderr/exit 事件转发通过 outbound adapter 发送到正确的飞书用户，不再依赖 senderId 作为飞书 target
- Approval 通知改用 outbound adapter：onApprovalNeeded 和 restart_timeout 通知通过 outbound adapter 投递
- cc_stop 时清理 sessionRoutes

### 关键决策
- OpenClaw 插件 API 没有 `api.sendMessage` 方法——这是飞书消息只输出到控制台而不发送到用户的根因
- 命令处理器 `return { text }` 的同步回复路径不受影响（OpenClaw 核心自动路由 PluginCommandResult 的 text 字段到飞书 outbound adapter）
- 异步消息（stdout 转发、审批通知、exit 通知）必须通过 `api.runtime.channel.outbound.loadAdapter()` 获取飞书 outbound adapter 的 `sendText` 方法发送——这是 OpenClaw 官方异步消息投递方式（参考 device-pair 插件实现）
- outbound adapter 缓存：FeishuMessenger 内部缓存 `_outboundCache`，避免每次 sendToUser 都调用 `loadAdapter`
- `sendText` 参数：`{ cfg: api.config, to: target, text: chunk, accountId, threadId }`——`to` 是飞书的 chat_id 或 open_id
- `ctx.to`（而非 `ctx.from`）作为异步消息投递目标——飞书 DM 场景下 `from` 是用户 open_id，`to` 也是用户 open_id（bot 不需要"发送给自己"）

### 遇到的问题
- e2e 测试 sentMessages 断言失败：`sendToUser` 改为 async 后，原同步调用 `messenger.sendToUser('user1', notification)` 返回 Promise 而非 number，需改为 `await messenger.sendToUser(...)`；`sentMessages[0].target` 改为 `sentMessages[0].to`（outbound adapter 参数名不同）
- e2e 重启通知测试的第二个 FeishuMessenger 实例使用旧版 `api.sendMessage` mock，缺少 `api.runtime.channel.outbound` 导致 `TypeError: Cannot read properties of undefined (reading 'channel')`；需为 restartApi 添加完整的 runtime mock
- plugin.test.js mockApi 缺少 `api.runtime` 属性，init() 中创建 FeishuMessenger 时 `_getOutbound` 调用失败——但因为 mock 模式下 FeishuMessenger 的 sendToUser 不被直接调用（命令只 return { text }），该问题在运行时不会触发，只在 e2e 集成测试中出现